# NUXP Shell Environment Configuration
#
# Copy this file to .env and modify as needed.
# Variables prefixed with VITE_ are exposed to the frontend code.

# Enable mock mode for frontend development without C++ plugin
# Set to "true" to use MockBridge instead of real plugin communication
VITE_USE_MOCK=false

# Plugin HTTP server URL
# The C++ plugin runs an HTTP server for frontend communication
VITE_PLUGIN_URL=http://localhost:8080
